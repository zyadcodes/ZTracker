//This will be the default screen style that should be used through out the app. It determines the background.
import colors from './colors';

export default {
    container: {
        flexDirection: 'column',
        alignItems: 'center',
        backgroundColor: colors.black,
        flex: 1
    }
};